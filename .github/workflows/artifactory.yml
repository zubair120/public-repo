name: "Setup JFrog CLI OIDC Example"
on: push

permissions:
  # This is required for requesting the OIDC token
  id-token: write
  # This is required for actions/checkout
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: zubair120/checkout@latest
      - name: Setup JFrog CLI
        uses: zubair120/setup-jfrog-cli@latest
        env:
          # JFrog platform url (for example: https://acme.jfrog.io)
          JF_URL: https://dev.packages.broadcom.com
          
        with:
          # Name of the OIDC provider as specified on the OIDC integration page in the JFrog Platform
          oidc-provider-name: githubcom
          oidc-audience: githubcom

      - name: Run JFrog CLI
        run: |
          echo "JF_URL" 
          echo "JF_URL_DEV"
          echo "oidc-provider-name"
          echo "oidc-audience"
          
          # Ping the server
          jf rt ping
          # Collect environment variables for the build
          jf rt bce
          # Collect VCS details from git and add them to the build
          jf rt bag
          # Publish build info
          jf rt bp
